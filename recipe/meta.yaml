{% set name = "primecountpy" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/primecountpy-{{ version }}.tar.gz
  sha256: 5b3c9b036e00c6c98220ea2294ea67865841ab243960153e4bbb61e1c4bda3cb

build:
  skip: true  # [py<311]
  skip: true  # [win]
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - cython                                 # [build_platform != target_platform]
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
  host:
    - cysignals
    - cython
    - pip
    - setuptools
    - primecount
    - python
  run:
    - cysignals
    - python

test:
  imports:
    - primecountpy

about:
  home: https://github.com/dimpase/primecountpy
  summary: Cython interface for C++ primecount library
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - saraedum
    - isuruf
